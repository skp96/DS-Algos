class TreeNode {

  constructor(value) {
    this.value = value;
    this.left = null;
    this.right = null; 
  }
};


const has_path = function(root, sum) {
};

const hasPathHelper = (root, sum, runningSum) => {
  
}

// Time Complexity: O(N);
// Space Complexity: O(N);